package httpserver.framework;

public class FrontControllerOld {
/*
    public static boolean processRequest(HttpRequest request, HttpResponse response) throws IOException {
        if (request.getPath().equals("/time")) {
            processTimeRequest(request, response);
            return true;
        }
        return false;

    }


    private static void processTimeRequest(HttpRequest request, HttpResponse response) throws IOException {
        response.setStatus(HttpStatus.OK);
        response.writeBody("<html><body>" + new Date() + "</body></html>");

    }*/
}
